name: "CI"
on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: cd src && npm ci
    - run: npm run build --if-present
    - run: npm test
